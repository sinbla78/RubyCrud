# Gemfile
source 'https://rubygems.org'

gem 'rails', '~> 7.0'       # Rails 프레임워크
gem 'pg', '~> 1.5'          # PostgreSQL 드라이버
gem 'bcrypt', '~> 3.1'      # 비밀번호 암호화
gem 'dotenv', '~> 2.8'      # 환경변수 관리

group :development do
  gem 'rspec', '~> 3.0'     # 테스트용
end
